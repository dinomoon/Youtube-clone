extends layouts/main

block content
  section.user__container
    .user__wrap
      img.user__img(src=user.avatarUrl)
      h2.user__name=user.name
      h3.user__email=user.email
      a.edit-profile(href=routes.editProfile(user.id)) Edit Profile